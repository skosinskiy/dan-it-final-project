language:
  java

jdk:
  - oraclejdk8

script:
  - mvn clean test
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar
after_success:
  - bash <(curl -s https://codecov.io/bash)

addons:
  sonarcloud:
    organization: "skosinskiy_dan-it-final-project"
